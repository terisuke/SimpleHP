{
  "name": "architecture-website",
  "version": "1.0.0",
  "description": "A simple architecture company website",
  "scripts": {
    "build": "mkdir -p dist && node copy-files.js"
  },
  "dependencies": {
    "netlify-lambda": "^2.0.16"
  }
}
